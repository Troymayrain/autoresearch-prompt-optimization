# uv is the primary dependency manager for this project.
# Keep this file only for compatibility with tools that still read requirements.txt.
google-genai>=1.0.0
anthropic>=0.40.0
openai>=1.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
openpyxl>=3.1.0
